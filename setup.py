from distutils.core import setup
from setuptools import find_packages

setup(name='',
      version ='0.1',
      author='Sushma'
      author_email='sush.kumari@fau.de',
      packages=find_packages(),
      install_requires=['numpy','Pillow','ipywidgets'])